{% extends '::base.html.twig' %}

{% block body -%}
    <div class="col-md-1">
        <a> </a>
    </div>
    <h1>Dodaj Komentarz</h1>
    <div class="row">
        <div class="col-md-1">
            <a> </a>
        </div>
        <div class="col-md-5">
            {{ form(form) }}
            <a href="{{ path('task') }}"><h5>Wróć do listy</h5></a>
            <a href="{{ path('coderslab_main_main') }}"><h5>Strona główna</h5></a>
            <a href="{{ path('fos_user_security_logout') }}"><h5>Wyloguj</h5></a>
        </div>
    </div>

{% endblock %}
