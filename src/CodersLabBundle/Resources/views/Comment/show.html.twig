{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Comment</h1>

    <table class="record_properties">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{ entity.description }}</td>
        </tr>
        <tr>
            <th>Date</th>
            <td>{{ entity.date|date('Y-m-d') }}</td>
        </tr>
        </tbody>
    </table>


    <a href="{{ path('task') }}"><h5>Wróć do listy</h5></a>
    <a href="{{ path('comment_edit', { 'id': entity.id }) }}"><h5>Edit</h5></a>
    {{ form(delete_form) }}

{% endblock %}
